#include <tunables/global>

profile sonuntius flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  capability,
  file,
  signal (send) set=(kill,term,int,hup,cont),
  network,
}
